# Eval package — Healthark GenAI Evaluation Framework (Initiative 4).
#
# Modules:
#   eval_config         — Rule-based evaluation configuration
#   eval_utils          — Rule-based scoring engine
#   eval_store          — JSON persistence for evaluation runs
#   eval_metrics        — BLEU, ROUGE, BERTScore, semantic similarity
#   eval_judge          — LLM-as-Judge rubric-based scoring
#   eval_rag            — RAGAS-style RAG evaluation metrics
#   benchmark_loader    — Benchmark dataset loading and validation
